# Frontend environment variables
# Absolute base URL for the backend API (no trailing slash)
# Example for production: https://api.ishori.com
# Example for VPS with port: https://vps.example.com:5000
VITE_API_BASE_URL=http://localhost:5000

# Optional: public base path if hosted in a subdirectory
# VITE_BASE_PATH=/
